---
# vars file for mysql
mysql_root_password: password
mysql_databases:
  - name: miniclient
    encoding: utf8mb4
    collation: utf8mb4_unicode_ci
mysql_users:
  - name: miniclient
    host: "%"
    password: password
    priv: "*.*:ALL"